import alumni from "../../data/alumni.json";
import { Page, ImageBlock, SourceLink } from "../../components/Content";
export const metadata={title:"Alumni | Kanatzidis Research Group"};
export default function Alumni(){const categories=[...new Set(alumni.map(p=>p.category))];return <Page active="people" eyebrow="Alumni" title="A community beyond the laboratory." description="An archive of former group members and shared research history."><section className="section"><div className="shell"><ImageBlock label="Past group photographs" className="group-photo-block"/><p className="source-note">Names and years are drawn from the original alumni directory. Years and categories are retained as listed; historical employment details are available in the original archive. <SourceLink path="alumni.html">Alumni & group photographs</SourceLink></p>{categories.map(c=><section className="directory-section" key={c}><h2>{c}</h2><div className="alumni-grid">{alumni.filter(p=>p.category===c).map(p=><div key={p.name}><strong>{p.name}</strong><span>{p.year}</span></div>)}</div></section>)}<a className="text-link dark-link" href="/kanatzidis-demo/people">← Group directory</a></div></section></Page>}
