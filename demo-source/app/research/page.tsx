import { ResearchImage } from "../components/ResearchImage";
import { Page, SourceLink } from "../components/Content";
import { research } from "../data/content";
export const metadata={title:"Research | Kanatzidis Research Group"};
export default function Research(){return <Page active="research" eyebrow="Research" title="Chemistry opens new possibilities." description="Explore the materials, synthesis methods and physical properties studied across the group."><section className="section"><div className="shell"><div className="direction-grid">{research.map((r,i)=><a className="direction-card" key={r.slug} href={`/kanatzidis-demo/research/${r.slug}`}><ResearchImage slug={r.slug}/><div className="direction-body"><span className="eyebrow dark">0{i+1} / {r.subtitle}</span><h2>{r.title} <span aria-hidden="true">↗</span></h2><p>{r.summary}</p></div></a>)}</div><div className="source-note"><SourceLink path="research.html">Research descriptions from the original group website</SourceLink><p>Representative paper figures are credited on each research page.</p></div></div></section></Page>}
